node_modules/
dist/
build/
.DS_Store
*.log
npm-debug.log*
.vite/

# Editor / OS
.vscode/
.idea/
*.swp

# Local Claude Code state (settings, skill symlinks, plugin checkout)
.claude/
.uipm/

# Environment / secrets — never commit
.env
.env.*
!.env.example

# Real Sidney UI — reference only, never commit or deploy
sidney-staging/

# Raw product screenshots Harry drops in for /guide (shipped as resized webp in public/assets/guide/)
Screenshots for Guide/
